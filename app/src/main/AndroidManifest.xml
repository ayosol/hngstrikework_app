<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.solom.hotel_csv_app">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UploadFileActivity" />
        <activity android:name=".ReadAndDisplayDataActivity" />
        <activity android:name=".ResponseActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/SplashSCreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ReadAndDisplayDataActivity_Java"
            android:label="@string/title_activity_read_and_display_data__java"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>